{
  "name": "batch_get_items",
  "description": "Retrieve multiple items from one or more DynamoDB tables in a single request. Can fetch up to 100 items.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "request_items": {
        "type": "object",
        "description": "Map of table names to keys array (e.g., {'TableName': {'Keys': [{'id': 'val1'}, {'id': 'val2'}]}})"
      }
    },
    "required": ["request_items"]
  }
}
